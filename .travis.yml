language: python
python:
  - '3.5'
  - '3.6'
# env:
#   - TO_RUN=TESTS
#   - TO_RUN=BUILD
# notifications:
#   email:
#     on_success: change
#     on_failure: always
before_install:
  # - pip install cython
  # - pip install numpy pandas scipy
  # - git clone https://github.com/facebookresearch/fastText.git
  # - cd fastText
  # - pip install .
  # - cd ..
  # - pip install scikit-learn
  - pip install git+https://github.com/facebookresearch/fastText.git@3b5fd293597de550a131d81436b9755902f39bb2
  - pip install codecov
install:
       pip install ".[test]" --process-dependency-links 
script: pytest
after_success:
  - codecov
